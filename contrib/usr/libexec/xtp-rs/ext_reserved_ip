# ==============================================================
# ext_reserved_ip – 额外保留 IP / 强制直连地址列表
# ==============================================================
# 此文件用于扩展 nftables 规则中的 reserved_ip 集合。
# 这些 IP 或 CIDR 网段将**不经过透明代理**，直接由内核路由。
#
# 适用场景：
#   - 上游 VPN 的 IP（避免代理循环）
#   - 内网专用服务器（如 NAS、打印机、内部 API）
#   - 特定公网 IP（如 CDN 节点、游戏服务器，若需直连）
#   - 任何不想被 xtp-rs 劫持的目的地址
#
# 格式规则：
#   - 每行一个 IPv4 地址或 CIDR 网段（如 192.168.1.0/24）
#   - 以 '#' 开头的行为注释，会被忽略
#   - 空行会被忽略（脚本自动跳过）
#   - 行尾**无需**手动加逗号，脚本会自动补上
#   - 不要在行内写多个 IP（一行只能一个）
#   - 支持末尾有空格（脚本会清理），但建议保持整洁
# ==============================================================
